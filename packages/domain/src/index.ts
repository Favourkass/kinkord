// @kinkord/domain — shared domain models (PMs) and contracts used by web + api.
// Populated as features land; the web app's src/domain moves here once the API consumes it.

export const DOMAIN_PACKAGE = "@kinkord/domain" as const;
