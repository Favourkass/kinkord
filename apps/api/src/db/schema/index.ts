// Drizzle schema barrel. Better Auth tables (user/session/account/verification)
// are generated into ./auth.ts by the auth task; profile and domain tables land
// alongside as features ship.

export {};
